# Requires the updated Mach GDN overlay and --mamba-ssm-cache-dtype float16.
# FP16 recurrent storage changes numerical precision; existing profiles stay FP32.
source "$(dirname "${BASH_SOURCE[0]}")/qwen38-checkpoint-long.env"
export VLLM_MACH_FP16_SSM_STATE=1
export VLLM_MACH_BA_OVERLAP_M16_M24=1
