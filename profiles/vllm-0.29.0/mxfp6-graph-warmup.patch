diff --git a/vllm/v1/worker/gpu/cudagraph_utils.py b/vllm/v1/worker/gpu/cudagraph_utils.py
index f6564da..468267a 100644
--- a/vllm/v1/worker/gpu/cudagraph_utils.py
+++ b/vllm/v1/worker/gpu/cudagraph_utils.py
@@ -318,6 +318,7 @@ class CudaGraphManager:
         self,
         create_forward_fn: CreateForwardFn,
         progress_bar_desc: str = "Capturing CUDA graphs",
+        capture_stream_warmup: Callable[[], None] | None = None,
     ) -> None:
         """Capture CUDA graphs.
 
@@ -329,6 +330,9 @@ class CudaGraphManager:
                 metadata during warmup.
         """
         with graph_capture(device=self.device):
+            if capture_stream_warmup is not None:
+                capture_stream_warmup()
+
             # Capture in order: PIECEWISE first, then FULL. PIECEWISE has larger
             # activations so FULL activations should fit in already allocated
             # buffers in the graph pool.
@@ -614,7 +618,24 @@ class ModelCudaGraphManager(CudaGraphManager):
 
             return forward_fn
 
-        super().capture(create_forward_fn, progress_bar_desc)
+        capture_sizes = [
+            desc.num_tokens for descs in self._capture_descs.values() for desc in descs
+        ]
+
+        def warmup_capture_stream() -> None:
+            from vllm_mach.mxfp6.warmup import warmup_mxfp6_sm120_stream
+
+            warmup_mxfp6_sm120_stream(
+                model,
+                capture_sizes,
+                self.vllm_config.model_config.dtype,
+            )
+
+        super().capture(
+            create_forward_fn,
+            progress_bar_desc,
+            capture_stream_warmup=warmup_capture_stream,
+        )
 
     def run_fullgraph(
         self, desc: BatchExecutionDescriptor
diff --git a/vllm/v1/worker/gpu_worker.py b/vllm/v1/worker/gpu_worker.py
index 65ea862..bd66f6b 100644
--- a/vllm/v1/worker/gpu_worker.py
+++ b/vllm/v1/worker/gpu_worker.py
@@ -524,6 +524,16 @@ class Worker(WorkerBase):
         """
         maybe_apply_startup_plan(self)
 
+        # MRV2 may capture profiling graphs before kernel_warmup runs.
+        from vllm_mach.mxfp6.warmup import warmup_mxfp6_sm120
+
+        capture_sizes = self.vllm_config.compilation_config.cudagraph_capture_sizes or []
+        warmup_mxfp6_sm120(
+            self.get_model(),
+            [1, self.scheduler_config.max_num_batched_tokens, *capture_sizes],
+            self.model_config.dtype,
+        )
+
         if kv_cache_memory_bytes := self.cache_config.kv_cache_memory_bytes:
             # still need a profile run which compiles the model for
             # max_num_batched_tokens
