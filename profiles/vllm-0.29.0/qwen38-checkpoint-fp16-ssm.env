# Keep a9's precision and scheduling choices across the dependency upgrade.
source "$(dirname "${BASH_SOURCE[0]}")/../vllm-0.28.0/qwen38-checkpoint-fp16-ssm.env"
# Standalone FlashInfer AR now defaults on in 0.29. Keep the previous standalone
# backend and reserve the FlashInfer workspace for Mach's fused collectives.
export VLLM_ALLREDUCE_USE_FLASHINFER=0
